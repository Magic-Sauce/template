import {
  renderEventTemplate,
  renderContentTemplate,
  EventPartial,
  ContentPartial,
} from "./templates";

export default {
  renderEventTemplate,
  renderContentTemplate,
};

export { EventPartial, ContentPartial };
